/**
 * Support Configuration Object.
 * 
 * @typedef {Object} SupportConfig
 * @property {string} email - Email address to reach support.
 * @property {string} phone - Phone number to reach support.
 * @property {string} webpage - Main support page
 */

const supportConfig = {
    email: "support@company.com",
    phone: "(xxx)xxx-xxxx",
    webpage: "http://www.company.com/support",
};

export default supportConfig; 
